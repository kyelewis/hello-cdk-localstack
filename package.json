{
  "name": "@hello-cdk-localstack/meta",
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm --recursive build",
    "format": "pnpm --recursive format",
    "start": "pnpm --recursive start"
  },
  "keywords": [] 
}
